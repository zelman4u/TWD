rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tagoloan Water District Municipal Cloud Collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

